services:
  medis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medis
    ports:
      - 3000:3000
    volumes:
      - ./videos:/videos
      - ./cookies:/cookies
    environment:
      - TZ=Asia/Makassar
      - MAX_QUEUE_LIMIT=5
    restart: unless-stopped
